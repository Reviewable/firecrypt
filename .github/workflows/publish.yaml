name: Publish to npm
on:
  push:
    branches:
      - release

permissions:
  id-token: write
  contents: write

jobs:
  publish:
    uses: reviewable/actions/.github/workflows/publish.yaml@master
  publish-tools:
    uses: reviewable/actions/.github/workflows/publish.yaml@master
    with:
      directory: tools
